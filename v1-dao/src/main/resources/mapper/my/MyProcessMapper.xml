<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zw.dao.mapper.my.MyProcessMapper">
  <resultMap id="BaseResultMap" type="com.zw.vo.process.ProcessResponseVo">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="gx_id" jdbcType="BIGINT" property="gxId" />
    <result column="user_id" jdbcType="BIGINT" property="userId" />
    <result column="corporation_id" jdbcType="BIGINT" property="corporationId" />
    <result column="price" jdbcType="DECIMAL" property="price" />
    <result column="price_add" jdbcType="DECIMAL" property="priceAdd" />
    <result column="index_key" jdbcType="SMALLINT" property="indexKey" />
    <result column="type" jdbcType="INTEGER" property="type" />
    <result column="start_time" jdbcType="TIMESTAMP" property="startTime" />
    <result column="end_time" jdbcType="TIMESTAMP" property="endTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
    <result column="state" jdbcType="INTEGER" property="state" />
    <result column="product_id" jdbcType="BIGINT" property="productId" />
    <result column="ticket_id" jdbcType="BIGINT" property="ticketId" />
    <result column="flag" jdbcType="SMALLINT" property="flag" />
    <association property="userName" column="user_id" javaType="Long" select="getUserName"></association>
  </resultMap>

  <select id="getUserName" parameterType="long" resultType="String">
    SELECT name FROM cm_user WHERE id=#{id}
  </select>

  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>

  <sql id="Base_Column_List">
    id, gx_id, user_id, corporation_id, price, price_add, index_key, type,
     start_time, end_time, update_time, state,  product_id,
     ticket_id, flag
  </sql>

  <select id="selectByExample" parameterType="com.zw.vo.process.ProcessResponseVo" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from zs_process
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>
</mapper>